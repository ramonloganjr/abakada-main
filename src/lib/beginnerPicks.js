// Curated "Start here" tools for first-time / non-technical visitors.
//
// These are the most universally approachable FOSS apps — each is a near drop-in
// replacement for software the audience already knows (Word, Chrome, Photoshop,
// Outlook, Media Player, a password manager…). The IDs below are verified to exist
// in tools.json; BeginnerPicks resolves them against live data and silently skips
// any that are missing, so the section degrades gracefully if the catalog changes.
//
// Order matters: it is the left-to-right reading order of the section.
// Kept to six so it forms a clean grid (3×2 desktop, 2×3 tablet) and doesn't
// re-create the "too many choices" problem this section exists to solve.
export const BEGINNER_PICK_IDS = [
  'libreoffice',  // ↔ Microsoft Office / Google Docs
  'firefox',      // ↔ Chrome / Edge
  'vlc',          // ↔ Windows Media Player / QuickTime
  'gimp',         // ↔ Adobe Photoshop
  'thunderbird',  // ↔ Microsoft Outlook
  'bitwarden',    // ↔ LastPass / 1Password
]
